{
  "name": "deepseek-harness-pkg",
  "version": "0.1.2-alpha.2",
  "description": "Packaged DeepSeek Harness (dsh) with pnpm patches, built for cross-platform distribution.",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "scripts": {
    "start": "dsh web",
    "build": "pnpm deploy --filter . --prod build_dir --legacy"
  },
  "dependencies": {
    "@deepseek-ai/dsh": "0.1.2-alpha.2",
    "koffi": "^3.1.4"
  }
}
