name: Sync & Release DeepSeek Harness

# 定期检测 npm 上的 @deepseek-ai/dsh 最新版本；发现新版本后自动调用
# release.yml 构建四个平台并发布 Release。上游 deepseek-ai/deepseek-harness
# 目前不发布 GitHub Release，同步信号以 npm dist-tag latest 为准。
on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:
    inputs:
      version:
        description: '手动指定要同步的 dsh 版本（留空自动取 npm latest）'
        required: false
        default: ''
      force:
        description: '即使版本未变化也强制重新构建'
        required: false
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.resolve.outputs.version }}
      changed: ${{ steps.resolve.outputs.changed }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Resolve target version
        id: resolve
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail

          target="${{ github.event.inputs.version }}"
          if [ -z "$target" ]; then
            target="$(npm view @deepseek-ai/dsh version)"
          fi
          echo "target version: $target"

          # 上次已发布的版本，从最新 release tag `dsh-<version>-<run_id>` 解析。
          last=""
          if gh release list --limit 1 --json tagName --jq '.[0].tagName' >/tmp/last_release.txt 2>/tmp/last_release.err; then
            tag="$(cat /tmp/last_release.txt)"
            last="${tag#dsh-}"
            last="${last%-*}"
          else
            echo "gh release list failed: $(cat /tmp/last_release.err)"
          fi
          echo "last released version: ${last:-<none>}"

          changed="false"
          if [ "$target" != "$last" ] || [ "${{ github.event.inputs.force }}" = "true" ]; then
            changed="true"
          fi
          echo "changed: $changed"

          echo "version=$target" >> "$GITHUB_OUTPUT"
          echo "changed=$changed" >> "$GITHUB_OUTPUT"

  build:
    needs: sync
    if: needs.sync.outputs.changed == 'true'
    uses: ./.github/workflows/release.yml
    with:
      dsh_version: ${{ needs.sync.outputs.version }}

  # Sync & Release 完成后，把 main 分支上的版本号（@deepseek-ai/dsh 依赖
  # 以及本仓库自身 version）同步为本次同步的版本，并实时推送到 main。
  update-main:
    needs: [sync, build]
    if: needs.sync.outputs.changed == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Update version on main
        env:
          TARGET: ${{ needs.sync.outputs.version }}
        run: |
          set -euo pipefail
          # 更新 @deepseek-ai/dsh 依赖为本次同步的版本（save-exact）
          npm pkg set "dependencies.@deepseek-ai/dsh=$TARGET"
          # 本仓库自身 version 同步为该版本
          npm version "$TARGET" --no-git-tag-version --allow-same-version
          # 尽量让 lockfile 保持同步（失败不阻塞提交）
          if command -v pnpm >/dev/null 2>&1 && [ -f pnpm-lock.yaml ]; then
            pnpm install --lockfile-only --ignore-scripts 2>/dev/null || true
          fi

      - name: Commit and push to main
        run: |
          if git diff --quiet && git diff --cached --quiet; then
            echo "no version changes to commit"
          else
            git add package.json pnpm-lock.yaml
            git commit -m "chore: sync version to ${{ needs.sync.outputs.version }}"
            git push origin main
            echo "pushed version ${{ needs.sync.outputs.version }} to main"
          fi


